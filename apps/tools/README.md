# apps/tools (optional)
If you want tools isolated from the marketing site, build a small app here and mount it at /tools via a rewrite. Otherwise, delete this and keep tools inside apps/web as islands.
