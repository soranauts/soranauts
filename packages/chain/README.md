# packages/chain
A typed facade around chain/index/dex operations. UI imports only this package.

## TODO
- Implement Substrate read adapter using @polkadot/api
- Add indexer adapter (SubQuery/Subsquid) behind env var
- Add Polkaswap quote adapter (HTTP/SDK) behind env var
